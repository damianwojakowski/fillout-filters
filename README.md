## Installing Fillout-Filters
To install, follow these steps:

```bash
npm install    # Installs depencendies
```

Running in DEV mode
```bash
npm run dev
```

To build and run
```bash
npm run build  # Builds the project
npm start      # Starts the project
```
